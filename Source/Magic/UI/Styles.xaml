<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:helpers="clr-namespace:Magic.UI.Helpers"
	xmlns:metro="clr-namespace:Elysium;assembly=Elysium">

	<Style x:Key="WizardStep" TargetType="{x:Type Button}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type ButtonBase}">
					<Border 
						x:Name="border" 
						Margin="24, 0, 24, 0"
						BorderThickness="0,0,0,5" 
						BorderBrush="{Binding Source={x:Static metro:Manager.DefaultAccentBrush}}">
						
						<TextBlock 
							Text="{TemplateBinding Content}" 
							Margin="0,0,0,15"
							Foreground="White"
							FontSize="21"
							FontFamily="Segoe UI Light" />
					</Border>

					<ControlTemplate.Triggers>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="BorderBrush" TargetName="border" Value="Gray"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="HeaderStyle" TargetType="{x:Type TextBlock}">
		<Setter Property="Foreground" Value="White" />
		<Setter Property="FontSize" Value="26" />
		<Setter Property="FontWeight" Value="Bold" />
		<Setter Property="FontFamily" Value="Segoe UI Light" />
	</Style>
	
	<helpers:BooleanToVisibilityConverter x:Key="boolToVisibilityConverter"></helpers:BooleanToVisibilityConverter>

</ResourceDictionary>